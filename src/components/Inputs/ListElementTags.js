import React from 'react';

const ListElementTags = (props) => {
    return (
        <div className=''>
            {props.label}
            {/* {close button} */}
        </div>
    );
};

export default ListElementTags;